# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 1. Beri tahu GitHub untuk masuk dan melakukan build di dalam folder Frontend
      - name: Install Dependencies and Build
        run: npm ci && npm run build
        working-directory: Frontend
        env:
                VITE_FIREBASE_API_KEY=AIzaSyCWAAX4eOHtK-kfq5Adtrzm7l9uABbqH1k
                VITE_FIREBASE_AUTH_DOMAIN=rsr-kelashan.firebaseapp.com
                VITE_FIREBASE_PROJECT_ID=rsr-kelashan
                VITE_FIREBASE_STORAGE_BUCKET=rsr-kelashan.firebasestorage.app
                VITE_FIREBASE_MESSAGING_SENDER_ID=205051201067
                VITE_FIREBASE_APP_ID=1:205051201067:web:49257dff5a593629390c6e
                VITE_FIREBASE_MEASUREMENT_ID=G-KCGBZYXPG1


      # 2. Beri tahu Firebase Action bahwa file firebase.json ada di dalam folder Frontend
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_RSR_KELASHAN }}
          channelId: live
          projectId: rsr-kelashan
          entryPoint: ./Frontend
